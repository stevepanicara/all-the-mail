PLACEHOLDER — see ./README.md. Replace this file's contents with the exact string Stripe provides at
Stripe Dashboard → Settings → Payment methods → Apple Pay → Add new domain → "Download verification file"
(or its successor location in the current Stripe UI).
Do NOT change the filename and do NOT edit the body with a newline added — paste the raw string byte-for-byte.
